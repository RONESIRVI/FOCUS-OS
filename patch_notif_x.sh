sed -i 's/Text(\n                                        text = notif.tag,/IconButton(onClick = { onDismissNotification(notif.id) }, modifier = Modifier.size(24.dp)) {\n                                        Icon(Icons.Default.Close, contentDescription = "Dismiss", tint = FocusTextSecondary)\n                                    }\n                                    Spacer(modifier = Modifier.width(8.dp))\n                                    Text(\n                                        text = notif.tag,/g' app/src/main/java/com/example/ui/dialogs/NotificationCenterDialog.kt
