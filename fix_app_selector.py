# Let's ensure App Selector shows up fine on schedule create screen,
# we already routed to it in NavGraph: `onNavigateToAppSelector = { navController.navigate(FocusRoutes.APP_SELECTOR) }`
# And when popping back, it goes back to `SCHEDULE_CREATE`.

# What about the title of the ScheduleCreateScreen?
