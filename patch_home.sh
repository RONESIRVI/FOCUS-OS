sed -i 's/onDismissNotification = { id -> /onClearAll = {\n                val allIds = scheduledSessions.map { it.id.toString() } + listOf("summary")\n                val newSet = dismissedNotificationIds + allIds\n                dismissedNotificationIds = newSet\n                sharedPrefs.edit().putStringSet("DISMISSED_NOTIFS", newSet).apply()\n            },\n            onDismissNotification = { id -> /g' app/src/main/java/com/example/ui/screens/HomeScreen.kt
